# 範例：查詢使用者資訊
# 這是一個基礎範例，請根據實際的 GraphQL schema 進行調整

# 當您從後端取得實際的 schema 後，請取消註解並修改以下查詢：

# query GetUser($id: ID!) {
#   user(id: $id) {
#     id
#     name
#     email
#   }
# }

# 範例：查詢所有使用者
# query GetAllUsers {
#   users {
#     id
#     name
#     email
#   }
# }

# 暫時的佔位查詢（可刪除）
query Placeholder {
	__typename
}
