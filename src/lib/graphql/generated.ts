/**
 * GraphQL 自動產生的型別檔案
 *
 * 此檔案會在執行 `pnpm graphql:codegen` 後自動產生
 * 請勿手動編輯此檔案
 *
 * 若要產生此檔案：
 * 1. 確保 GraphQL API 端點可連線
 * 2. 執行 `pnpm graphql:codegen`
 */

// 佔位型別，實際型別會在程式碼產生後覆蓋
export type Scalars = {
	ID: string;
	String: string;
	Boolean: boolean;
	Int: number;
	Float: number;
};

// 當 GraphQL schema 可用時，此檔案會自動產生完整的型別定義
